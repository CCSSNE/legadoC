# 下载/缓存架构标准

本文是阅读 C 当前下载/缓存实现的架构契约和设计标准。后续修改以本文为准，不以历史迁移过程、提交说明或临时排障记录为准。

## 1. 范围与总原则

缓存域只包含正文、评论、音频和视频：

```text
TEXT + BODY    正文
TEXT + REVIEW  评论
AUDIO/VIDEO + MEDIA  媒体
```

导出是独立领域，不进入 `CacheCoordinator`。

统一的是任务所有权、业务状态、命令、终态、通知、日志和恢复；正文、评论、音频、视频仍保留各自合理的底层执行器。

唯一数据流：

```text
入口 -> CacheCoordinator -> CacheTaskStore -> worker adapter
     -> 底层执行器 -> lease 回报 -> CacheTaskStore
     -> UI / 通知 / AppLog
```

禁止出现：

```text
UI -> Service / 底层执行器
Service -> Service
worker -> 全局终态或最终通知
旧协程 -> 新任务状态
```

## 2. 职责边界

### `CacheCoordinator`：唯一任务 Owner

负责接收和校验 `CacheRequest`、创建 session/task、分派 worker、处理暂停/恢复/取消、建立 BODY 到 REVIEW 的章节依赖、接收 reader 刷新意图，以及在 Store 接受终态后触发通知。

Coordinator 不执行网络、WebView、ExoPlayer 或章节解析。

### `CacheTaskStore`：唯一业务状态源

负责并持久化 `CacheSnapshot`，包括 session、task、unit、生命周期、结果、generation、暂停/恢复/取消、进程恢复和 session 汇总。所有异步回报都必须经过 Store 的 lease 校验。

执行器可以有队列、Future、WebView、播放器和下载进度等运行态，但不能以它们作为第二业务账本，也不能直接写最终任务状态。

### worker adapter：执行翻译层

```text
TextBodyWorkerAdapter -> CacheBookModel / CacheBookService
ReviewWorkerAdapter   -> ReviewSnapshotManager / ReviewCacheService
MediaWorkerAdapter    -> AudioCacheTaskManager / 媒体执行器
```

adapter 取得 Coordinator 分配的 lease，把命令翻译给执行器，再把 unit 更新或终态转换为带 lease 的 Store 回报。adapter 不创建第二个 session，不推断全局终态，不直接发通知。

### Android Service：执行宿主

`CacheBookService` 和 `ReviewCacheService` 只承载前台服务和长生命周期执行：接收 task/lease、启动或停止 adapter、调用 `startForeground()`、在宿主销毁时交给 dispatcher/Coordinator 处理恢复或异常。

Service 不拥有任务、不拥有最终状态、不控制另一个 Service、不决定最终通知。是否合并 Service 不是本标准的要求。

## 3. 数据模型

`CacheRequest` 的核心字段：

```text
source = CACHE_ACTIVITY | CACHE_MANAGE | READER | AUTO_PRECACHE | SYSTEM
kind   = TEXT | AUDIO | VIDEO
phase  = BODY | REVIEW | MEDIA
bookUrl, bookName, units, reviewEnabled
```

一个 `CacheSessionState` 包含一个或多个 `CacheTaskState`：

- 正文是 `TEXT + BODY` task。
- 正文产生的评论是同一 session 内的 `TEXT + REVIEW` task。
- REVIEW task 只包含 BODY 成功的章节。
- 音频和视频使用 `MEDIA` phase，各自按 task 管理。
- task 的所有 unit 必须属于同一本书。

Coordinator 必须拒绝非法组合，例如媒体使用 BODY、非文本使用 REVIEW、或未授权入口直接提交 REVIEW。

### unit 依赖

```text
CacheUnitKey(bookUrl, chapterIndex)
  -> BODY unit
  -> REVIEW unit（仅 BODY 成功时创建）
```

unit 状态为 `PENDING`、`RUNNING`、`SUCCEEDED`、`FAILED`、`REVIEW_ELIGIBLE`、`REVIEW_BLOCKED`、`NOT_APPLICABLE`、`CANCELLED`。

- BODY 失败只阻断同一章节 REVIEW，不阻断其它章节。
- REVIEW 失败不改变该章节已经成功的 BODY。
- BODY 终态通知前，Coordinator 必须补建可用 REVIEW task。
- reader 对同书同章节的活动 REVIEW 请求必须复用现有 task。

`REVIEW_ELIGIBLE`、`REVIEW_BLOCKED`、`NOT_APPLICABLE` 是 unit 层兼容/依赖状态，不是 task/session 生命周期；没有持久化迁移前不得擅自删除。

### lease

```text
CacheWorkerLease
  sessionId
  taskId
  generation
```

新 task 从 `generation=0` 开始；每次 acquire/reclaim、暂停后恢复、进程恢复或 Service 重新接管都取得新 generation。旧 lease 更新必须被 Store 丢弃并记录 `STALE_UPDATE_DROPPED`，不能静默回退写入新 task。

## 4. 生命周期和结果

### 生命周期

```text
QUEUED -> RUNNING
RUNNING -> PAUSING -> PAUSED -> RUNNING
RUNNING -> CANCELLING -> CANCELLED
QUEUED -> CANCELLING -> CANCELLED
PAUSED -> CANCELLING -> CANCELLED
RUNNING -> COMPLETED | FAILED
INTERRUPTED -> RUNNING | CANCELLING | CANCELLED
```

终态为 `COMPLETED`、`FAILED`、`CANCELLED`。`PAUSING` 和 `CANCELLING` 是过渡态；`INTERRUPTED` 是进程恢复时等待重新接管的状态。

### 结果

结果为 `SUCCEEDED`、`PARTIAL`、`FAILED`、`CANCELLED`，与 lifecycle 分开：

| 实际情况 | lifecycle | result |
| --- | --- | --- |
| 所有 unit 成功 | `COMPLETED` | `SUCCEEDED` |
| unit 全部执行，部分成功、部分失败 | `COMPLETED` | `PARTIAL` |
| unit 全部执行，但全部失败 | `COMPLETED` | `FAILED` |
| 异常结束且仍有 unfinished unit | `FAILED` | `FAILED` 或按已完成 unit 汇总为 `PARTIAL` |
| 取消确认完成 | `CANCELLED` | `CANCELLED` |

规则：

- `PARTIAL` 不是生命周期。
- 正常执行完但结果全失败必须是 `COMPLETED + FAILED`。
- 只有仍有 unfinished unit 的异常收尾才使用 `lifecycle=FAILED`。
- Store 不接受带 unfinished unit 的成功收尾；异常收尾会把未完成 unit 明确记为 `FAILED`。
- `finishTask()` 不接受 `CANCELLED`；取消必须经过 `beginCancel()` 和 `confirmCancelled()`。
- 终态写入后，旧 lease 和后续 worker 回报一律拒绝。

session 状态由 task 汇总：活动 task 存在时 session 非终态；全部取消为 `CANCELLED`；有取消且有其它结果为 `PARTIAL`；失败与成功/部分结果并存为 `PARTIAL`；全部终态且无部分结果时才汇总为 `SUCCEEDED` 或 `FAILED`。

## 5. 入口和命令

以下入口只能调用 `CacheCoordinator` 并观察 `CacheCoordinator.snapshot`：

| 入口 | 职责 |
| --- | --- |
| `CacheActivity` | 正文提交、状态观察、暂停/恢复/取消 |
| `CacheManageActivity/ViewModel` | 正文/媒体提交、状态观察、任务命令 |
| 阅读页 | 自动正文缓存、章节评论刷新意图 |
| `MainViewModel` | 自动预缓存提交 |
| 通知 action receiver | 将通知动作转为 Coordinator 命令 |
| 音频/视频页面 | 观察 Store 派生状态 |

公共 UI 端口只有：

```text
snapshot / submit / pause / resume / cancel
```

UI/ViewModel 不得直接调用 `CacheBookService`、`ReviewCacheService`、`CacheBookModel`、`ReviewSnapshotManager`、`AudioCacheTaskManager` 或内部 `CacheWorkerPort`。

## 6. 通知和日志

`CacheNotificationBridge` 是唯一缓存通知构造/更新路径：

- 进度从 `CacheCoordinator.snapshot` 生成。
- action 只发送 Coordinator 命令。
- Store 接受终态后才发送终态通知。
- 终态结果优先使用 session 汇总结果。
- foreground host 使用 Bridge 提供的基础通知。
- worker、执行器和 Service 不得直接调用 `notificationManager.notify()`。

通知不是状态源，通知丢失、延迟或进程重启都不能改变 Store 事实。

缓存事件统一进入 `AppLogCacheLogSink`，包括：

```text
SESSION_CREATED / SESSION_RECOVERED
TASK_QUEUED / TASK_STARTED / TASK_RECLAIMED
TASK_PAUSING / TASK_PAUSED / TASK_RESUMED
TASK_CANCELLING / TASK_CANCELLED / TASK_FINISHED
UNIT_UPDATED / UNIT_FAILED
STALE_UPDATE_DROPPED
PERSISTENCE_LOAD_FAILED / PERSISTENCE_SAVE_FAILED
WORKER_DISPATCH_FAILED / REQUEST_ACCEPTED
```

日志必须携带可用的 session/task 标识和原因；异常不能用静默 catch、默认成功或无依据回退隐藏。

## 7. 持久化和进程恢复

`CacheTaskStore` 持久化 `CacheSnapshot`。恢复时：

1. 读取失败记录 `PERSISTENCE_LOAD_FAILED` 并暴露错误。
2. `RUNNING` task 变为 `INTERRUPTED`。
3. `PAUSING` task 收敛为 `PAUSED`。
4. `CANCELLING` task 收敛为 `CANCELLED`。
5. dispatcher 扫描 `QUEUED` 和 `INTERRUPTED` task。
6. task 重新取得新 generation lease 后交给 adapter。

Service 不能自行读快照并绕过 Store 重启任务，恢复不能复用旧 lease。

## 8. 并发、隔离和禁止事项

并发是执行器内部运行策略，不能生成第二个 Owner 或状态源：

- REVIEW 必须按 task/章节隔离，不能按整本书误伤其它 task。
- 暂停/取消按 session/task 定向；“全部任务”才遍历活动 task。
- 媒体可以继续使用 ExoPlayer 或已有下载核心，但终态只能通过 lease 回报 Store。
- 新增并发设置必须同时核对界面默认值、`AppConfig` 默认值和实际读取点。

禁止：UI 直接启动执行器；Service 互控；worker/Service 自建终态；worker 直发最终通知；EventBus/通知/StateFlow 充当事实源；整本书决定评论依赖；忽略 generation；静默吞错、无依据截断或保护性上限；为形式上的单 Service 重写合理执行器。

## 9. 修改验收清单

涉及下载/缓存的修改必须确认：

- 新入口经 `CacheCoordinator`。
- 新业务状态进入 `CacheTaskStore`。
- 执行器只通过 lease 回报。
- BODY/REVIEW 按章节 unit 表达依赖。
- 暂停、恢复、取消走 Coordinator 命令。
- lifecycle/result 符合本标准，包括 `COMPLETED + FAILED` 语义。
- 通知由 `CacheNotificationBridge` 从快照生成。
- 请求、unit、终态、过期回报和持久化错误进入 AppLog。
- 进程恢复重新取得 generation。
- 没有新增 Owner、状态源、通知源或 Service 控制路径。

关键实现位置：

```text
app/.../help/cache/CacheContracts.kt
app/.../help/cache/CacheCoordinator.kt
app/.../help/cache/CacheTaskStore.kt
app/.../help/cache/CacheWorkerDispatcher.kt
app/.../help/cache/TextBodyWorkerAdapter.kt
app/.../help/cache/ReviewWorkerAdapter.kt
app/.../help/cache/MediaWorkerAdapter.kt
app/.../help/cache/CacheNotificationBridge.kt
app/.../help/cache/CacheLogSink.kt
app/.../service/CacheBookService.kt
app/.../service/ReviewCacheService.kt
```

## 10. 当前实现验证记录

这是验证结果，不是第二套架构说明：

- `appC` 编译成功，`VERSION_CODE=10719`，APK 内部版本为 `3.26.082223c`，包名为 `io.legado.app.c`，ABI 为 `arm64-v8a`，签名校验通过。
- `CacheTaskStoreTest` 7 项通过，失败、错误、跳过均为 0。
- 仅在 `tools/android-dev/target.json` 指定的雷电实例 `127.0.0.1:5555` 完成安装、启动和“缓存/导出 -> 离线缓存”页面进出回归，无 `FATAL EXCEPTION`。
- 模拟器没有书籍和活动缓存任务，因此没有通过 UI 产生真实的暂停/恢复/取消任务；这些生命周期和终态规则由 Store 单测覆盖。

当前架构的标准形态是：一个任务 Owner、一个业务状态源、三类专业执行器、两个 Android 执行宿主，所有入口和反馈路径统一收口。
