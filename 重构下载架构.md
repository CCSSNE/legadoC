# 计划如下

这五条建议我全部吸收，而且它们把方案里最容易再次出问题的地方补齐了。最终方案不再是“一个大状态枚举 + 两个服务互相调用”，而是把生命周期、结果、章节依赖、通知和异步代际彻底分开。

**最终边界**

统一的是“缓存任务领域”，不是把所有代码塞进一个类：

```text
CacheCoordinator
├─ TextBodyWorker
├─ ReviewWorker
└─ MediaWorker
```

正文、评论、音频、视频都属于缓存域；导出继续是独立的 `ExportBookService`，不并入缓存协调器。

`CacheActivity`、`CacheManageActivity`、阅读页、自动预缓存都只是提交请求和观察状态的入口，不能再直接操作 `CacheBookService`、`ReviewCacheService`、`AudioCacheTaskManager` 或线程池。

**状态契约**

生命周期状态和汇总结果分成两个字段。

生命周期只允许：

```text
QUEUED
  ├─ RUNNING
  │    ├─ PAUSED ─→ RUNNING
  │    ├─ CANCELLING ─→ CANCELLED
  │    ├─ SUCCEEDED
  │    └─ FAILED
  └─ CANCELLED
```

`PARTIAL` 不进入生命周期状态，而是结果字段：

```text
CacheResult:
SUCCEEDED
PARTIAL
FAILED
CANCELLED
```

例如：

```text
正文任务生命周期：SUCCEEDED
正文任务结果：PARTIAL
正文：38/40 成功，2 章失败
```

这样“执行过程完成”和“章节结果是否全成功”不会混在一起。

**章节级依赖**

正文和评论不能按整本书做粗粒度判断，必须以 `CacheUnit` 为单位：

```text
ChapterUnit
├─ bodyStatus
├─ reviewStatus
├─ bodyError
└─ reviewError
```

每章独立处理：

```text
正文成功
  → 该章评论进入 REVIEW_ELIGIBLE

正文失败
  → 该章评论为 REVIEW_BLOCKED

评论成功
  → 该章完成

评论失败
  → 该章正文仍保持成功，评论单独失败
```

40 章中正文成功 38 章时，只对这 38 章进入评论阶段。最终：

```text
BODY result   = PARTIAL
REVIEW result  = 根据 38 章实际结果汇总
SESSION result = PARTIAL
```

评论失败不能污染正文成功状态。

**任务与代际**

`sessionId` 不够，还必须有 `generation`。

```text
WorkerLease:
├─ sessionId
├─ taskId
└─ generation
```

规则：

- 每次新会话生成新的 `sessionId`。
- 暂停后恢复、停止后重启、服务重新接管任务时，`generation + 1`。
- worker 启动时拿到自己的 lease。
- 所有异步回调更新状态时，必须携带 lease。
- `CacheTaskStore` 发现当前 generation 不一致，直接丢弃旧更新，并写日志：

```text
丢弃过期缓存更新：
session=...
task=...
workerGeneration=...
currentGeneration=...
```

这样旧会话不会在新会话开始后污染进度、通知和最终结果。

**任务所有权**

唯一的任务 Owner 是 `CacheCoordinator`。

第一阶段不强行合并 Android Service，而是先把现有实现改成 adapter：

```text
CacheCoordinator
    ↓
TextBodyAdapter        → 旧 CacheBookService
ReviewAdapter           → 旧 ReviewCacheService
MediaAdapter            → AudioCacheTaskManager
```

这一步完成后：

- 旧 Service 不再拥有全局任务状态。
- 旧 Service 不再自己决定最终通知。
- 旧 Service 不再互相发送停止命令。
- 所有状态通过 Coordinator 写入 `CacheTaskStore`。

等 adapter 路径稳定后，再决定是否合并为一个 `CacheForegroundService`。Service 合并是后续迁移结果，不是第一阶段的前提。

**通知契约**

不强制永远只有一个 `notificationId`，但必须只有一个通知来源：

```text
Worker
  └─ 禁止直接调用 notificationManager

CacheCoordinator
  └─ CacheNotificationRenderer
       ├─ summary notification
       └─ 可选 child notification
```

第一版只做一个缓存汇总通知，后面如果多本书同时缓存，再增加子通知。

无论有几个通知，它们都必须：

- 来自 `CacheTaskStore` 的快照。
- 由同一个 renderer 生成。
- 暂停、继续、停止都发送 Coordinator 命令。
- 完成后不撤回。
- `setAutoCancel(false)`。
- 终态只写一次。

正文、评论、音频、视频都不再自己维护通知生命周期。

**并发契约**

不再使用模糊的 `threadCount` 表示所有下载并发。

最终分成：

```text
cacheBodyConcurrency
    正文同时下载多少章

reviewChapterConcurrency
    同时处理多少个评论章节

reviewButtonConcurrency
    一个章节内部同时处理多少评论按钮

mediaConcurrency
    音频/视频同时处理多少任务
```

其中：

- `threadCount` 保留为全局网络功能配置，不再代表正文缓存线程数。
- 用户看到的“评论缓存并发数”建议代表章节并发。
- 评论按钮并发单独命名，不能再伪装成章节并发。
- 当前评论固定的 4 个 worker 必须纳入明确配置或明确的 worker policy。
- 所有设置的界面默认值和 `AppConfig` 读取默认值必须完全一致。

**统一状态来源**

两个界面保留，但不再各自维护状态：

```text
CacheActivity
    └─ CacheUiViewModel → CacheTaskStore

CacheManageActivity
    └─ CacheUiViewModel → CacheTaskStore
```

它们的区别只在于：

- `CacheActivity`：书籍批量操作。
- `CacheManageActivity`：章节选择、缓存文件管理、媒体管理。

文本、评论、音频、视频的状态都从同一个 `CacheUiState` 读取。`EventBus` 不再作为下载状态源，只保留真正的跨模块事件。

**日志契约**

所有缓存 worker 都只发结构化事件：

```text
REQUEST_ACCEPTED
TASK_STARTED
PHASE_STARTED
UNIT_STARTED
UNIT_SUCCEEDED
UNIT_FAILED
PAUSED
RESUMED
CANCELLING
CANCELLED
TASK_FINISHED
SESSION_FINISHED
STALE_UPDATE_DROPPED
SERVICE_START_FAILED
```

由统一日志 sink 写入 `AppLog`。音频、视频不能再只维护 `StateFlow` 和通知而不进日志。

**迁移顺序**

1. 建立 `CacheTaskStore`、状态枚举、`CacheUnit`、`WorkerLease` 和 `CacheCoordinator` 契约。
2. 把现有正文、评论、媒体实现包成 adapter，不改变下载行为。
3. 将所有入口改成 `CacheCoordinator.submit()`。
4. 删除 `MainViewModel` 直接调用 `CacheBook.startProcessJob()` 的旁路。
5. 把正文→评论关系迁移到章节级 `CacheUnit`。
6. 统一暂停、停止、恢复和终态通知。
7. 让两个界面共同观察 `CacheTaskStore`。
8. 重新整理并发设置和文案。
9. 最后再删除旧的 Service 间通信、旧状态字段、旧通知更新和无用 EventBus。

验收标准也很明确：

```text
全库只能有一个缓存任务 Owner
全库只能有一个缓存状态来源
worker 不得直接发通知
worker 不得直接写最终任务状态
旧 generation 不得写入新 session
正文失败只阻断对应章节评论
所有缓存结果都必须进入 AppLog
所有入口都必须走 CacheCoordinator
```

这就是最终方案。上一版 `ba8cd26` 继续作为正文→评论生命周期修复基线，但不再继续在它上面堆逻辑。下一阶段只先建立契约和 Coordinator，不同时重写页面、服务、媒体和设置，避免再次进入半迁移状态。





# 最终状态

对。现在可以这样理解：**最初规划的核心架构已经基本落地，而且实现过程中确实做了几处比原方案更合理的修正。**

最重要的变化不是“把几个下载类重新命名”，而是把原来混在一起的 **任务所有权、执行器、状态、通知、界面** 真正拆开了。你当初文档里定的核心原则就是：统一的是“缓存任务领域”，不是把所有下载代码塞进一个大类；正文、评论、音频、视频归同一个缓存域，而导出继续独立。

现在最终成品，我建议你把它理解成下面这样：

```text
                        用户入口
        ┌──────────────┬──────────────┬──────────────┐
        │              │              │              │
  CacheActivity   CacheManage     阅读页       自动预缓存
        │              │              │              │
        └──────────────┴──────┬───────┴──────────────┘
                              │
                              ▼
                      CacheCoordinator
                    「唯一任务指挥中心」
                              │
                              ▼
                       CacheTaskStore
                     「唯一任务状态账本」
                              │
             ┌────────────────┼────────────────┐
             │                │                │
             ▼                ▼                ▼
     TextBodyWorker      ReviewWorker      MediaWorker
             │                │                │
             ▼                ▼                ▼
   CacheBookService   ReviewCacheService  AudioCacheTaskManager
             │                │                │
             ▼                ▼                ▼
        CacheBook      评论抓取/WebView      ExoPlayer
```

### 你最需要理解的一句话

**Coordinator 管“要做什么、现在是什么状态”；Worker 管“具体怎么做”。**

以前的问题恰好就是反过来了。

以前大概是：

```text
CacheBookService
既下载
又管状态
又管停止
又管通知
又触发评论

ReviewCacheService
又有自己的一套状态
又有自己的通知
又控制正文 Service

AudioCacheTaskManager
又单独一套状态
又单独通知

页面
又自己推断任务有没有结束
```

所以谁都“拥有”一点下载任务，最后谁也说不清任务到底归谁。

现在变成：

```text
CacheCoordinator
    ↓
“这个任务属于我”

CacheTaskStore
    ↓
“状态以我这里为准”

Worker / Service
    ↓
“我只负责执行”
```

这正是原方案里“唯一任务 Owner 是 `CacheCoordinator`，旧 Service 最终只是 adapter / execution host”的目标。

而从现在的实际代码看，这个边界已经真的形成了。`CacheBookService` 现在明确写成 **Coordinator-owned text BODY workers 的 Android execution host**，主要负责接 lease、补目录和跑正文执行器，不再维护旧的全局下载状态。 `ReviewCacheService` 也是同样的定位，只负责承载评论 worker。

---

## 第二个关键：Store 就像“数据库账本”

以后不要再问：

> CacheBookService 觉得下载完了吗？

也不要问：

> AudioCacheTaskManager 现在是什么状态？

正确问题只有：

> **CacheTaskStore 里这个 Task / Unit 是什么状态？**

这点也是原设计非常关键的一部分：两个界面可以保留，但文本、评论、音频、视频都应该读取同一个状态来源。

所以你可以把 `CacheTaskStore` 想成缓存系统的“总账”。

例如一章：

```text
第 10 章

BODY
    SUCCEEDED

REVIEW
    FAILED

MEDIA
    不适用
```

Store 里会保留这些事实。

Coordinator 再根据这些事实算：

```text
BODY task:
COMPLETED + SUCCEEDED

REVIEW task:
COMPLETED + PARTIAL / FAILED

整个 session:
COMPLETED + PARTIAL
```

这也是这次重构过程中比早期想法更成熟的地方：**生命周期和结果被拆成了两个维度。**

你的规划文件里已经明确规定，`PARTIAL` 不是生命周期状态，而是结果；生命周期负责说明“还在不在执行”，结果负责说明“执行质量如何”。

所以现在不会再有以前那种很别扭的：

```text
任务到底是完成了？
还是失败了？
部分失败算不算完成？
```

现在可以同时表达：

```text
生命周期：COMPLETED
结果：PARTIAL
```

这其实是这套架构里非常重要的一次改进。

---

## 第三个关键：不是“整本书成功才评论”，而是章节级流水线

这个也是后来方案比最初粗粒度设计更好的地方。

现在逻辑不是：

```text
一本书正文全结束
↓
开始整本书评论
```

而是概念上：

```text
第1章 BODY 成功 ─────→ 第1章 REVIEW eligible
第2章 BODY 失败 ─────→ 第2章 REVIEW blocked
第3章 BODY 成功 ─────→ 第3章 REVIEW eligible
第4章 BODY 成功 ─────→ 第4章 REVIEW eligible
```

也就是说：

> **正文和评论之间的依赖属于 Chapter Unit，而不是两个 Service 之间的依赖。**

这就是为什么以前 `CacheBookService → ReviewCacheService` 那种“服务互相知道对方状态”的设计终于可以被删掉。

原方案已经把这个规则明确成：正文失败只阻断对应章节评论，评论失败也不能污染该章已经成功的正文。

这比原来稳定得多。

---

## 第四个关键：generation 是“防诈尸机制”

这个可能是整套架构里最技术、但也最有价值的东西。

现在每个 worker 实际拿的不是一句：

```text
“你去下载第10章”
```

而是一个：

```text
WorkerLease
├─ sessionId
├─ taskId
└─ generation
```



你可以把它理解成**任务版本号**。

例如：

```text
generation 1
正在下载
    ↓
用户暂停
    ↓
恢复
generation 2
重新接管
```

这时候 generation 1 的旧协程突然回来汇报：

```text
“我下载成功了！”
```

Store 一看：

```text
你是 generation=1
现在已经是 generation=2
```

直接丢弃。

所以旧 worker 再也不能“诈尸”把新任务的状态改坏。

这就是之前我们一直担心的：

```text
旧 worker
↓
新 session 已开始
↓
旧回调迟到
↓
污染新状态
```

现在从架构上有了正式防线。规划文件也明确要求旧 generation 不能写入新 session。

---

## 第五个关键：Service 现在只是“宿主”，不是“大脑”

这是你理解最终架构时最容易混淆的地方。

你可能会问：

> 既然已经有 Coordinator，为什么 `CacheBookService` 和 `ReviewCacheService` 还存在？

因为 Android 上长时间下载仍然需要一个可靠的执行宿主。

所以现在：

```text
CacheCoordinator
= 大脑

CacheTaskStore
= 账本

WorkerAdapter
= 翻译层 / 驱动层

CacheBookService
ReviewCacheService
= Android 执行容器

CacheBook
ReviewSnapshotManager
AudioCacheTaskManager
= 真正干活的底层实现
```

Service 没必要为了“架构纯洁”强行删掉。

这一点其实也是实现过程中对原计划的一次正确修正。最早我们讨论过以后可能合并成一个 `CacheForegroundService`，但你的方案后来已经明确：**Service 合并不是前提，adapter 稳定后再决定是否有必要。** 

现在看下来，我反而认为：

> **只要 Service 不再拥有业务状态，就没有必要为了“只剩一个 Service”而硬合并。**

两个 Android execution host 本身不是坏架构。

真正危险的是两个 Service 各自拥有一套业务状态。

这个已经基本解决了。

---

## 通知也同样可以这样理解

通知现在不应该再属于：

```text
正文 Service
评论 Service
Audio Manager
```

而应该属于：

```text
CacheTaskStore
      ↓
CacheNotificationBridge
      ↓
Notification 203
```

原设计的要求就是：通知只能从统一状态快照生成，暂停、继续、停止都回到 Coordinator，终态只写一次。

现在实际 `CacheNotificationBridge` 已经在做这个角色。

前面我说还要收尾的那一点，只是两个 Android Service 为了 foreground requirement 仍然自己构造了一张基础通知。

这是**实现层最后一点边界问题**，不是架构方向没完成。

---

所以如果让我把最终架构压缩成一张最容易记住的图，就是：

```text
                    ┌────────────────────┐
                    │       UI / 入口     │
                    │ 页面 / 阅读 / 自动  │
                    └─────────┬──────────┘
                              │ submit
                              ▼
                    ┌────────────────────┐
                    │  CacheCoordinator  │
                    │   唯一任务 Owner   │
                    └─────────┬──────────┘
                              │
                              ▼
                    ┌────────────────────┐
                    │   CacheTaskStore   │
                    │   唯一状态来源     │
                    └─────────┬──────────┘
                              │
             ┌────────────────┼────────────────┐
             ▼                ▼                ▼
          BODY              REVIEW           MEDIA
             │                │                │
       Text Worker      Review Worker     Media Worker
             │                │                │
      CacheBook         ReviewSnapshot    AudioCache
             │                │                │
             └────────────────┼────────────────┘
                              │
                    实际网络 / WebView /
                       ExoPlayer 执行
                              │
                              ▼
                         回报 lease
                              │
                              ▼
                         TaskStore
                         /       \
                        /         \
                      UI        Notification
                                  +
                                AppLog
```

**数据只能这样循环：**

```text
入口
→ Coordinator
→ Worker
→ 底层执行
→ lease 回报
→ Store
→ UI / 通知 / 日志
```

不能再出现：

```text
UI → CacheBook
UI → AudioCacheTaskManager
Service → Service
Worker → 自己发最终通知
Worker → 自己决定全局任务完成
旧协程 → 随便改状态
```

而这正好就是你原来那份方案最后定的验收标准：一个 Owner、一个状态源、旧 generation 不得污染新 session、正文失败只阻断对应评论、所有入口走 Coordinator。

所以可以把这次重构最终理解为：

> **以前是“四套下载逻辑凑在一起”；现在是“一个缓存任务系统，下面挂三种专业执行器”。**

正文、评论、音频、视频仍然有各自不同的技术实现——这是应该的；但**任务是谁的、状态在哪、什么时候算结束、暂停停止听谁的、通知看谁的、日志从哪里出**，现在都已经被统一到了同一个架构里。

这才是这次重构真正完成的东西。

# 2026-08-23 最终源码验收

本节以当前工作区源码为准，不以 commit 描述或旧的完成说明为准。

## 当前架构

- `CacheCoordinator` 是缓存任务唯一 Owner；所有正文、评论、音频、视频入口都经 `submit()` 或 Coordinator 的同域命令进入。
- `CacheTaskStore` 是唯一业务任务状态源，持久化 `CacheSnapshot`，负责 lifecycle、result、unit、generation、暂停/恢复/取消和进程恢复。
- `TextBodyWorkerAdapter`、`ReviewWorkerAdapter`、`MediaWorkerAdapter` 只把 Store lease 翻译给既有执行器；`CacheBookService`、`ReviewCacheService` 只做 Android execution host。
- `AudioCacheTaskManager`、`CacheBookModel`、`ReviewSnapshotManager` 仍保留执行过程中所需的队列、Future、WebView 和下载进度，但 UI 不读取它们，且它们不能写最终 Coordinator 状态。
- `CacheNotificationBridge` 是唯一缓存通知构造/更新路径；通知动作回到 Coordinator，worker 和 Service 不再各自维护业务通知。
- 所有任务更新携带 `sessionId/taskId/generation`，Store 丢弃过期更新并写 `STALE_UPDATE_DROPPED`；快照恢复会把运行中任务变为 `INTERRUPTED` 后重新取得 lease。

## 对照最初计划

已落实：唯一 Owner、唯一状态源、三类执行器分层、章节级正文→评论依赖、统一暂停/恢复/取消/终态、统一通知、AppLog 事件、generation 防迟到回写、两个缓存界面共同观察 Store、进程快照恢复，以及所有 UI/自动预缓存入口改走 Coordinator。

合理调整：没有为了形式上只剩一个 Service 而合并 Android 宿主；正文和评论最终以同一 session 下的两个 task 表达，评论 task 只包含正文成功的章节；媒体执行器继续使用现有 ExoPlayer 缓存核心，Store 只接管任务语义和可见状态。

## 本轮发现并修复

- 正文 registry 完成时遗漏清理 `managedBooks`，会让后续阅读页永久跳过评论缓存；现已统一随 binding 清理。
- 正文完成通知早于 REVIEW task 创建，可能短暂发出错误终态；现由 Coordinator 在终态通知前补建依赖 task。
- dispatcher 在取消/旧 lease 竞态下可能无条件补发失败通知；现只在 Store 接受终态写入后通知。
- REVIEW 阅读页入口原先直接写旧队列并启动 Service；现经 Coordinator 建立/复用 REVIEW task，刷新意图也经 Coordinator 命令写入。
- REVIEW 执行器原先按整本书拒绝并发、取消会误伤同书其它任务；现改为按 Coordinator task/章节隔离，取消和暂停定向清理队列。
- Store 不再允许带未完成 unit 的成功终态；执行异常会把未完成 unit 统一记为失败，保留正确的 `PARTIAL` 结果。

## 静态验收结论

未发现仍可从 UI/ViewModel 直接操作 `CacheBookService`、`ReviewCacheService`、`AudioCacheTaskManager` 或 Review 队列的旁路；未发现 Service 互控、重复通知源、旧 generation 回写新 task、正文失败阻断其它章节评论或缓存状态由 EventBus 充当事实源的问题。保留的执行器内部状态不是第二个业务账本，属于实现所需的在途资源状态。

静态架构重构已达到可以停止扩张、进入正式编译和运行验证的状态。编译/测试/模拟器结果不在本节预先假设，待本轮正式验证完成后补记。
